Objective of the course:

Node.js and Express.js (it's use)
Submit forms
Validate and sanitze user input
Registration / log in / log out
Middleware
Cookies
JSON WebTokens
SQLite
Configure a Linux server
Deploy our app to our server

Configure each url pattern in the js (server) "In translation configure each link"

In order to access username and passwords input, you need to enable it in express: app.use(express.urlencoded({ extended: true }))
the databas table deleted on it's own and always fetch
in using the token store in different file .env and it need to be in all cap, not the file name what's inside it.

Dependencies:
json webtolken and dotenv
cookie-parser
